auth_modules:
  db: # Set this to any name you want
    type: userpass
    userpass:
      username: postgres
      password: example
    options:
      # options become key=value parameters of the DSN
      sslmode: disable